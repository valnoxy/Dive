namespace Dive.UI.Initialization
{
    /*
    public class Action
    {
        public static void Tweaks(Configuration config)
        {
            // Going through the IF-ELSE hell >:)
            if (config.Telemetry.DisableCei)
            {
                if (!AutoInit.Telemetry.DisableCEI())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableAit)
            {
                if (!AutoInit.Telemetry.DisableAit())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableCeip)
            {
                if (!AutoInit.Telemetry.DisableCeip())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableDcp)
            {
                if (!AutoInit.Telemetry.DisableDcp())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableLicenseTelemetry)
            {
                if (!AutoInit.Telemetry.DisableLicenseTel())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableDeviceCensus)
            {
                if (!AutoInit.Telemetry.DisableDeviceCensus())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
        }

        public static void Telemetry(Configuration config)
        {
            // Going through the IF-ELSE hell >:)
            if (config.Telemetry.DisableCei)
            {
                if (!AutoInit.Telemetry.DisableCEI())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableAit)
            {
                if (!AutoInit.Telemetry.DisableAit())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableCeip)
            {
                if (!AutoInit.Telemetry.DisableCeip())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableDcp)
            {
                if (!AutoInit.Telemetry.DisableDcp())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableLicenseTelemetry)
            {
                if (!AutoInit.Telemetry.DisableLicenseTel())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
            if (config.Telemetry.DisableDeviceCensus)
            {
                if (!AutoInit.Telemetry.DisableDeviceCensus())
                {
                    // log error
                    CurrentAction.ErrorCounter++;
                }
            }
        }

    }
    */
}
